{
  "ClapDerive": {
    "prefix": "cli",
    "body": [
      "use clap::Parser;",
      "",
      "#[derive(Parser, Debug)]",
      "#[command(author, version, about, long_about = None)]",
      "pub struct Cli {",
      "    /// Input string",
      "    #[arg(short, long)]",
      "    input: String,",
      "",
      "    /// Verbose mode",
      "    #[arg(short, long)]",
      "    verbose: bool,",
      "}"
    ]
  }
}
